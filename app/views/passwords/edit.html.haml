%h1.heading.text-center Change your password
%p.text-center
  Your password has been reset.
  %br
  Choose a new password below.
#edit-password.row
  .col-12.col-sm-8.offset-sm-2.col-md-6.offset-md-3.col-lg-4.offset-lg-4
    = form_for :password_reset, url: user_password_path(@user, token: @user.confirmation_token), html: { method: :put } do |f|
      = f.password_field :password, placeholder: "Password", class: "password-input form-control"
      = f.submit "Save this password", class: "btn btn-primary btn-block"
